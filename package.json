{
  "name": "Teikei",
  "version": "0.0.7",
  "description": "An interactive map for community supported agriculture.",
  "scripts": {
    "kill": "killall node & killall ruby ; echo 'killed all running processes'",
    "banner": "asciify -f ivrit Teikei",
    "start:dev": "npm run -s kill && npm run -s banner && nf start -j Procfile-dev",
    "start:prod": "npm run -s kill && npm run -s banner && nf start -j Procfile -p 3000",
    "start": "npm run -s start:dev",
    "build:client": "cd client && npm build",
    "build:server": "bundle exec rake assets:precompile",
    "copy:assets": "cp dist/*.js public/ && cp dist/*.css public/",
    "build": "npm run -s clean && npm run -s build:client && npm run -s copy:assets && npm run -s build:server",
    "clean": "rm -rf dist && bundle exec rake assets:clobber && rm -f public/vendor.js public/app.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/teikei/teikei"
  },
  "keywords": [
    "csa",
    "farming",
    "agriculture"
  ],
  "author": "Simon Jockers, Daniel Mack, Tobias Preuß, Christian Rijke",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/teikei/teikei/issues"
  },
  "homepage": "https://github.com/teikei/teikei",
  "devDependencies": {
    "asciify": "^1.3.5",
    "foreman": "^1.4.1"
  }
}
