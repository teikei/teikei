{% extends "./layouts/base.njk" %}

{% block body %}
<p class="lead">{{ t('admin_audit.changes' }}</p>
<spacer size="16"></spacer>

<callout class="secondary">
{% for r in report %}
  <row>
    <columns large="6">
      <p>
        {{ r }}
      </p>
    </columns>
  </row>
{% endfor %}
</callout>

{% endblock %}

