{% extends "./layouts/base.njk" %}

{% block body %}
<h1>{{ t('confirmation_instructions.headline', { name: user.name, originName: originName})}}</h1>
<p>
  {{ t('confirmation_instructions.content', {email: user.email, originName: originName}) }}
</p>
<ul>
  <li>
    <a href="{{ origin }}{{ baseurl }}/?confirmation_token={{ user.verifyToken }}">{{ t('confirmation_instructions.linktext') }}</a>
  </li>
</ul>
<p>
{{ t('confirmation_instructions.alternative') }}
</p>
<ul>
  <li>
    {{ origin }}{{ baseurl }}/?confirmation_token={{ user.verifyToken }}
  </li>
</ul>
{% endblock %}
