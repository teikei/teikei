export const MAP_SIDEBAR_WIDTH_PX = 500;

/**
 * Editor mode widens the drawer on `lg+` so long forms have room, while list
 * and detail modes keep `MAP_SIDEBAR_WIDTH_PX`. The map stays visible/interactive
 * beside it (needed for address geocoding preview).
 */
export const MAP_EDITOR_WIDTH_PX = 680;
