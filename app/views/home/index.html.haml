- content_for :header do
  = render "shared/header"

%main
  .container

    %article.content
      %h1= @intro_block.title

      = layout_render_textblock @intro_block

    %aside.sidebar

      .info

        %h2= @howitworks_block.title

        = image_tag 'share.jpg'

        = layout_render_textblock @howitworks_block

= render "shared/footer"


