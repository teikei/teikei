<div class="entrylist-item row">
  <div class="entrylist-name seven columns">
    <h3><%= name %></h3>
    <em><%= city %></em>
    <ul class="button-group radius even two-up entrylist-controls">
      <li><a href="#" class="edit-entry button small">Bearbeiten</a></li>
      <li><a href="#" class="delete-entry button small">Löschen</a></li>
    </ul>
  </div>
  <div class="five columns preview-map">
    <% if (type === 'Farm') { %>
    <img src="/assets/marker-farm.svg" class="preview-marker" />
    <% } else { %>
    <img src="/assets/marker-depot.svg" class="preview-marker" />
    <% } %>
    <div class="alert-box alert"></div>
  </div>
</div>
