<article class="map-popup">
  <h3><%= name %></h3>
  <em><%= city %></em>

  <% var translatedProducts = Teikei.Util.translateProducts(vegetable_products, animal_products, beverages); %>
  <p>
  <% if (translatedProducts.length > 0) { %>
    <%= translatedProducts.join(", ") %>
  <% } %>
  </p>

  <button class="button small details" title="Öffnet die Details dieses Eintrags">Details</button>
  <button class="button small network neigbor-spacer" title="Visualisiert verbundene Höfe und Gruppen">Netzwerk anzeigen</button>

</article>
