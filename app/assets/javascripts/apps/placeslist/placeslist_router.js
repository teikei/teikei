Teikei.module("PlacesList", function(PlacesList, Teikei, Backbone, Marionette, $, _) {

  PlacesList.Router = Backbone.Marionette.AppRouter.extend({
    controller: PlacesList.Controller,

    appRoutes: {
    }

  });

});
