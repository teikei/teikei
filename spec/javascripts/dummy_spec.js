
describe("Teikei", function() {
  it("should be contained in the window object.", function() {
    expect(window.Teikei).toBeDefined();
  });
});

describe("jQuery", function() {
  it("should be contained in the window object.", function() {
    expect(window.jQuery).toBeDefined();
  });
});
